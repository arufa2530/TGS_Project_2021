using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DriveController : MonoBehaviour
{
    public bool IsLoadCD;

    // Start is called before the first frame update
    void Start()
    {
        IsLoadCD = false;
    }
}
